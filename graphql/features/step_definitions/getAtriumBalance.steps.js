const { Given, When, Then} = require('@cucumber/cucumber');

Given('I have a valid customerId, terminalId, accountMode, accountNumber, and cardNumber', function () {
});

When('I call the getAtriumBalance function', async function () {
});

Then('I should receive a successful response with my balance details', function () {
});

Given('I have an invalid customerId, terminalId, accountMode, accountNumber, or cardNumber', function () {
});

Then('I should receive an error response balance retrieval', function () {
});