const { Given, When, Then, } = require('@cucumber/cucumber');

Given('I have a valid customerId, cardNumber, accountNumber, terminalId, currency, amount, accountMode, and device', function () {
});

When('I call the sendAtriumTransaction function', async function () {
});

Then('I should receive a successful response with the transaction details', function () {
});

Given('I have an invalid customerId, cardNumber, accountNumber, terminalId, currency, amount, accountMode, or device', function () {
});

Then('I should receive an error response transaction', function () {
});